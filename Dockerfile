
# Используем легковесный образ Python
FROM python:3.12-alpine

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем исходный код
COPY . .

# Открываем порт, на котором работает приложение (в вашем коде это 5000)
EXPOSE 5000

# Команда для запуска
CMD ["python", "hw.py"]
